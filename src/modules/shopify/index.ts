import type { HoardcoreModuleManifest } from '../types'

export const shopifyModule: HoardcoreModuleManifest = {
  id: 'shopify',
  name: 'Shopify',
  description: 'Collect and normalize public Shopify catalog data.',
  status: 'planned',
}
